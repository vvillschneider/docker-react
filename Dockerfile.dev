FROM node:16-alpine

USER node

WORKDIR '/app'

COPY --chown=node:node package.json .

RUN npm install

#RUN sudo mkdir -p /app/node_modules/.cache && chmod -R 777 /app/node_modules/.cache

RUN sudo chown -R $(whoami) /app/node_modules/


COPY --chown=node:node . .

CMD ["npm", "run", "start"]